<label>Name:</label>
<h3><%=@doctor.name%>
<ul>
    <%@doctor.appointments.each do |t|%>
    <label><%= link_to t.patient.name, patient_path(t.patient)%>
    <li><%=t.appointment_datetime.strftime("%B %d, %Y at %k:%M")%></li>
    
    <%end%>
 </ul>
